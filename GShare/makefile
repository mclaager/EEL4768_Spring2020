# Compiler
CC = g++

# Target Executable
TARGET = SIM

# Source Code
SOURCE = main.cpp

# Builds the executable
$(TARGET) : $(SOURCE)
	$(CC) -o $(TARGET) $(SOURCE)

# Cleans any existing files
clean:
	rm -f $(TARGET)
